export interface BrightnessForm {
    brightness: number;
    autoBrightness: boolean;
    nightShift: boolean;
    autoLock: string;
    raiseToWake: boolean;
    textSize: string;
    boldText: boolean;
    displayZoom: boolean;
}
